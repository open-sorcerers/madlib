import L from './List'

pair :: a -> b -> <a, b>
export pair = (key, val) => <key, val>

fst :: <a, b> -> a
export fst = (tuple) => (where(tuple)
  is <a, _>: a
)

snd :: <a, b> -> b
export snd = (tuple) => (where(tuple)
  is <_, b>: b
)
