import IO from "IO"

interface Show a {
  show :: a -> String
}

instance Show Number {
  show = (num) => #- "Number: " + num.toString() -#
}

instance Show String {
  show = (s) => "String: " ++ s
}

show(3) |> IO.log
show("3") |> IO.log
