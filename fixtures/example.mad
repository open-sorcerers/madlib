import W  from "Wish"
import IO from "IO"
import F  from "Function"

pipe(
  W.of,
  W.map((x) => x + 1),
  W.chain((x) => W.of(3)),
  W.fulfill(
    F.always(()),
    pipe(IO.log, F.always(()))
  )
)(3)
